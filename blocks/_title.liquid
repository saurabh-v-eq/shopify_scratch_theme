<h3>{{ block.settings.text }}</h3>

{% schema %}
{
  "name": "Title",
  "settings": [
    { "type": "text", "id": "text", "label": "Title" }
  ],
  "presets": [
    {
      "name": "Title"
    }
  ]
}
{% endschema %}
