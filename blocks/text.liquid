<p {{ block.shopify_attributes }}>
  {{ block.settings.text }}
</p>

{% schema %}
{
  "name": "Text",
  "settings": [
    { "type": "text", "id": "text", "label": "Text" }
  ],
  "presets": [
    {
        "name": "Text"
    }
  ]
}
{% endschema %}
